apiVersion: apps/v1
kind: Deployment
metadata:
  name: cherrybooker-redis
  labels:
    app: cherrybooker-redis
spec:
  replicas: 1
  selector:
    matchLabels:
      app: cherrybooker-redis
  template:
    metadata:
      labels:
        app: cherrybooker-redis
    spec:
      containers:
        - name: redis
          image: redis:7-alpine
          ports:
            - containerPort: 6379
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 200m
              memory: 256Mi
---
apiVersion: v1
kind: Service
metadata:
  name: redis
  labels:
    app: cherrybooker-redis
spec:
  selector:
    app: cherrybooker-redis
  ports:
    - name: redis
      port: 6379
      targetPort: 6379
